version: "3.9"

services:
  api:
    build:
      context: ./api-recepcao
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    depends_on:
      - db

  frontend:
    build:
      context: ./web-admin
      dockerfile: Dockerfile
    ports:
      - "81:80"
    depends_on:
      - api
