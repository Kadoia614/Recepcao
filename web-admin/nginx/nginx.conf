server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name recepcao;

    # 🖥️ SPA Recepção
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }
}
